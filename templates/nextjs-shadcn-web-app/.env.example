# ===========================================
# 环境变量配置模板
# 使用方法：cp .env.example .env.local
# ===========================================

# 后端 API 地址；留空时请求当前站点的相对路径。
# NEXT_PUBLIC_ 变量会打包到浏览器，不能存放密钥或其他敏感信息。
NEXT_PUBLIC_API_URL=
